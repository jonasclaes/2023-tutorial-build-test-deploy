name: Deploy

on:
    push:
        branches:
            - main

jobs:
    Deploy:
        needs: test.Test
        runs-on: ubuntu-latest
        steps:
            -   uses: actions/checkout@v3
            -   name: Deploy to Production
                run: yarn deploy
